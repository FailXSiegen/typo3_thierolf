[page["uid"] in [3,67]]
  temp.SLIDER = CONTENT
  temp.SLIDER {
   table = tt_content
   select {
     pidInList = {$SLIDERID}
     where = hidden = 0 and deleted = 0
   }  
  }

}
[else]                                                                                                 
 temp.SLIDER = TEXT
 temp.SLIDER.data = ""

[global]

